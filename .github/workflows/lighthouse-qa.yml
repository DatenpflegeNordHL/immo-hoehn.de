name: Lighthouse QA

on:
  push:
    branches:
      - redesign/coastal-blueprint
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: lighthouse-coastal-blueprint
  cancel-in-progress: true

jobs:
  lighthouse:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Use Node 22
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install locked dependencies
        run: npm ci --no-audit --no-fund

      - name: Fetch WordPress 02 offer media
        env:
          STRATO_SSH_HOST: ${{ secrets.STRATO_SSH_HOST }}
          STRATO_SSH_USER: ${{ secrets.STRATO_SSH_USER }}
          STRATO_SSH_PRIVATE_KEY_B64: ${{ secrets.STRATO_SSH_PRIVATE_KEY_B64 }}
          STRATO_SSH_KNOWN_HOSTS: ${{ secrets.STRATO_SSH_KNOWN_HOSTS }}
        run: bash scripts/fetch-wp02-media.sh

      - name: Verify WordPress 02 property media
        run: node scripts/property-media-preflight.mjs

      - name: Build local audit site
        env:
          PUBLIC_SITE_NOINDEX: 'false'
        run: npm run build

      - name: Start local preview
        run: |
          npm run preview -- --host 127.0.0.1 --port 4321 > /tmp/astro-preview.log 2>&1 &
          echo $! > /tmp/astro-preview.pid
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:4321/ >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/astro-preview.log
          exit 1

      - name: Run Lighthouse audits
        shell: bash
        run: |
          mkdir -p lighthouse-results
          ROUTES=("home:/" "verkaufen:/immobilie-verkaufen/" "grundstueck:/grundstueck-verkaufen/" "bewertung:/immobilienbewertung/" "immobilien:/immobilien/" "rosenhagen:/immobilien/grundstuecke-rosenhagen-623-743/" "rosenhagen-premium:/immobilien/baugrundstuecke-rosenhagen-1599-1999/" "poetenitz:/regionen/poetenitz/" "travemuende:/regionen/travemuende/" "priwall:/regionen/priwall/")
          for item in "${ROUTES[@]}"; do
            name="${item%%:*}"
            route="${item#*:}"
            npx --yes lighthouse@13.4.1 "http://127.0.0.1:4321${route}" \
              --quiet --chrome-flags="--headless --no-sandbox --disable-dev-shm-usage" \
              --output=json --output-path="lighthouse-results/${name}-mobile.json"
            npx --yes lighthouse@13.4.1 "http://127.0.0.1:4321${route}" \
              --quiet --preset=desktop --chrome-flags="--headless --no-sandbox --disable-dev-shm-usage" \
              --output=json --output-path="lighthouse-results/${name}-desktop.json"
          done

      - name: Summarize and enforce quality thresholds
        run: node scripts/summarize-lighthouse.mjs lighthouse-results

      - name: Upload Lighthouse reports
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: lighthouse-${{ github.sha }}
          path: lighthouse-results/
          retention-days: 14
