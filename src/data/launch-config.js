// Launch-safe publication switches for Höhn Immobilien.
// Property data remains in the repository as verified historical source material,
// but is not advertised as current inventory until a fresh Höhn mandate/status is confirmed.
export const PUBLISH_PROPERTY_LISTINGS = false;
